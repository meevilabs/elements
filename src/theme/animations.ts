import { Animations } from '../types';
import { LoadingCircle } from '../assets/animations';

export default {
  circularLoading: LoadingCircle,
  buttonLoading: LoadingCircle,
  linearLoading: LoadingCircle,
} as Animations;
