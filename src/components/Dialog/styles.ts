import styled from 'styled-components/native';
import { Dialog as SimpleDialog } from 'react-native-simple-dialogs';

export const Wrapper = styled(SimpleDialog).attrs({
  contentStyle: {
    minHeight: '35%',
  },
})``;
