import styled from 'styled-components/native';
import { Animated, TouchableOpacity } from 'react-native';

export const Wrapper = styled(Animated.View)``;

export const TouchAnimation = styled(TouchableOpacity)``;
